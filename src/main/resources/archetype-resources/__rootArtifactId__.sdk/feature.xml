#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8"?>
<feature id="${rootArtifactId}.sdk"
         label="${fullName} Feature"
         version="${eclipseVersion}"
         provider-name="${vendorName}">
    
   <description>
      ${fullName}
   </description>

   <copyright url="${vendorUrl}">
      Copyright (C) 2013 ${vendorName}. All rights reserved.
   </copyright>

   <license url="license.html">
      %license
   </license>
    
    <plugin
            id="${rootArtifactId}.dsl"
            download-size="0"
            install-size="0"
            version="${eclipseVersion}"
            unpack="false"/>
    
    <plugin
            id="${rootArtifactId}.ui"
            download-size="0"
            install-size="0"
            version="${eclipseVersion}"
            unpack="false"/>
            
</feature>
